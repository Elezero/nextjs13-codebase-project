'use client'

const ClientComponent = () => {
	return (
		<section>
			<h3>Client component</h3>
			<button onClick={() => alert('Hello aliens')}>Button</button>
		</section>
	)
}

export default ClientComponent
